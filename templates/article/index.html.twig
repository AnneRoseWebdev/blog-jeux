{% extends 'base.html.twig' %}

{% block title %}Blog - Articles{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="text-center mb-4">🎮 Les derniers articles du blog</h1>

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        {% for article in articles %}
            <div class="col">
                <div class="card h-100 shadow-sm border-0">
                    <img src="{{ article.image }}" class="card-img-top" alt="{{ article.titre }}" style="height: 200px; object-fit: cover;">
                    <div class="card-body">
                        <h5 class="card-title">{{ article.titre }}</h5>
                        <p class="card-text text-muted" style="font-size: 0.9em;">{{ article.createdAt|date('d/m/Y') }}</p>
                        <p class="card-text">{{ article.contenu[:150] ~ '...' }}</p>
                        <a href="{{ path('app_article_show', {'id': article.id}) }}" class="btn btn-primary btn-sm">Lire plus</a>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col">
                <p>Aucun article trouvé.</p>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}