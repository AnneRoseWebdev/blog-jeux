<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}


        <style>
    body {
        background-color: #cceeff;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .game-list-container {
        background-color: white;
        padding: 40px;
        max-width: 900px;
        margin: auto;
        border-radius: 12px;
        box-shadow: 0 8px 16px rgba(0,0,0,0.1);
    }

    h1 {
        font-weight: bold;
        color: #333;
    }

    .table-custom {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0 10px;
    }

    .table-custom th {
        background-color: #004466;
        color: white;
        padding: 10px;
        text-align: center;
        border-radius: 6px 6px 0 0;
    }

    .table-custom td {
        background-color: #f1f9ff;
        padding: 12px;
        text-align: center;
        border-radius: 6px;
    }

    .btn-view,
    .btn-edit {
        text-decoration: none;
        padding: 6px 12px;
        border-radius: 6px;
        font-weight: bold;
        margin-right: 8px;
    }

    .btn-view {
        background-color: rgba(0, 0, 255, 0.587);
        color: white;
    }

    .btn-edit {
        background-color: #ffc107;
        color: black;
    }

    .btn-view:hover {
        background-color: #0056b3;
    }

    .btn-edit:hover {
        background-color: #e0a800;
    }
</style>


    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_home') }}">Mon Blog Jeux</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_jeu_index') }}">Jeux</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_article_index') }}">Blog</a>
                        </li>   
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_article_index') }}">Articles</a>
                        </li>
                            
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link text-success fw-bold" href="#">👤 {{ app.user.userIdentifier }}</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-danger" href="{{ path('app_logout') }}">Se déconnecter</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                            </li>
                        {% endif %}
                    </ul>
                    
                </div>
            </div>
        </nav>
        
        <div class="container mt-4">
            {% block body %}{% endblock %}
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<footer class="bg-dark text-light py-4 mt-5">
    <div class="container text-center">
        <p class="mb-1">© {{ "now"|date("Y") }} Mon Blog Jeux. Tous droits réservés.</p>
        <p class="mb-0">
            <a href="{{ path('app_home') }}" class="text-decoration-none text-light mx-2">Accueil</a> |
            <a href="{{ path('app_jeu_index') }}" class="text-decoration-none text-light mx-2">Jeux</a> |
            <a href="{{ path('app_contact') }}" class="text-decoration-none text-light mx-2">Contact</a>
        </p>
    </div>
</footer>


    </body>    
</html>
